=GlobalMenuNotificationServerNew=

When a server is created, it will find all windows on current screen with the name MenuClientName, and send them this notification

||Param1 || Server's messaging window's xwin id (whose name is MenuServerName )|| 